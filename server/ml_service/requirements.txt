# AgriRent Hub — ML Service Requirements
# Install with: pip install -r requirements.txt

# Core ML
ultralytics>=8.3.0          # YOLO11 (includes PyTorch detection + classification)
numpy>=1.24.0

# Web server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9     # Required for file uploads in FastAPI

# Image processing
Pillow>=10.0.0

# Video processing (optional — enables /detect-video endpoint)
opencv-python-headless>=4.9.0

# Gemini Vision API fallback
google-generativeai>=0.5.0

# Training (optional — only needed for train_yolo11.py)
kaggle>=1.6.0
gdown>=4.7.0
